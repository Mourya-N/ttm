# Copy this to .env.local for local development
# DO NOT commit .env.local — it's in .gitignore

# Only needed if you want to override the Vite proxy (dev)
# For production Railway deployment, set this in Railway's Variables tab
VITE_API_URL=
