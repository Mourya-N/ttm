# Application
spring.application.name=team-task-manager

# Port — Railway injects $PORT automatically
server.port=${PORT:8080}

# MongoDB Atlas — set MONGODB_URI as Railway env variable
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://nmourya236:QBmR1qWlCmEjsRtn@cluster0.b50r9lr.mongodb.net/team_task_manager?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=team_task_manager

# JWT — set JWT_SECRET as Railway env variable (min 256-bit / 32 chars)
app.jwt.secret=${JWT_SECRET:TTM_SuperSecretKey_2024_ChangeInProduction_MustBe256BitsLong}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS — comma-separated list of allowed frontend URLs
# Set CORS_ORIGINS in Railway to your frontend URL, e.g.:
# https://your-frontend.up.railway.app,http://localhost:5173
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Logging
logging.level.com.ttm=INFO
logging.level.org.springframework.security=WARN
